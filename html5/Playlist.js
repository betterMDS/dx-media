define([
	"dojo/_base/declare"
], function(declare){

	return declare('dx-media.html5.Playlist', [], {
		// big TODO here.
		// 		Unsure if this will just handle a playlist as an in-memory
		// 		object or if it will also have some kind of GUI.
	});

});
